Package: ${assembly.name}
Version: ${project.version}
Section: misc
Priority: optional
Architecture: all
Maintainer: Lukas Roedl <lukas.roedl@ait.ac.at>
Description: ${assembly.description}
Pre-Depends: debconf (>= 0.5) | debconf-2.0
Depends: python3, python3-pip, postgresql | postgresql-10 | postgresql-11 | postgresql-12 | postgresql-13, dos2unix
Provides: ${assembly.name}
Conflicts: ${assembly.name}
Replaces: ${assembly.name}